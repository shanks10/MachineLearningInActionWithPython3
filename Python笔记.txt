1.array.shape返回一个元组，元组中的每个元素表示相应的数组每一维的长度
建立一个4×2的矩阵c, c.shape[1] 为第一维的长度，c.shape[0] 为第二维的长度。
c = array([[1,1],[1,2],[1,3],[1,4]])  
>>> c.shape  
(4, 2)  
>>> c.shape[0]  
4  
>>> c.shape[1]  
2  
若是2维，则array.shape[0]返回行数，array.shape[1]返回列。